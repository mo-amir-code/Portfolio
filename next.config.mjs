/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["drive.google.com", "avatars.githubusercontent.com", "res.cloudinary.com"]
    }
};

export default nextConfig;
